---
// Components
// - Layout
import Layout from '../layouts/Layout.astro'
// - UI
import Hero from '../components/blocks/hero/PageHeader.astro'
import FAQ from '../components/blocks/FAQ/FaqSticky.astro'
import TextImage from '../components/blocks/basic/TextImage.astro'
import CTA from '../components/blocks/CTA/BasicDark.astro'

// Content
import SEOData from '../data/json-files/seoData.json'
import FAQData from '../data/json-files/faqData.json'
import FAQContent from '../data/json-files/faqContent.json'

// Add this import
import { seoConfig } from '../config/seo'

// Filter FAQ categories
const faqPricing = FAQData.filter((faq) => faq.category === 'pricing')
const faqIntegrations = FAQData.filter((faq) => faq.category === 'integrations')
---

<Layout title={SEOData.faq.title} description={SEOData.faq.description}>
    <Hero 
        title={SEOData.faq.headertitle} 
        text={SEOData.faq.headertext} 
    />
    <FAQ
        title={FAQContent.pricing.title}
        text={FAQContent.pricing.text}
        data={faqPricing}
    />
    <TextImage
        title={FAQContent.valueSection.title}
        alttext={FAQContent.valueSection.alttext}
        text={FAQContent.valueSection.text}
        image={FAQContent.valueSection.image}
        mobileImage={FAQContent.valueSection.mobileImage}
        imagePosition="left"
        offsetImage
        classes="bg-neutral-50 dark:bg-neutral-900 lg:!py-64"
    />
    <FAQ
        title={FAQContent.integrations.title}
        text={FAQContent.integrations.text}
        data={faqIntegrations}
    />
    <CTA />
</Layout>
